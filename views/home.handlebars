{{#each posts as |post| }}
<div class = 'post'>
    <h2>{{post.title}}</h2>
    <p>
        {{post.content}}
    </p>
    <div>by {{post.user.username}}</div>
    <div>{{format_date post.createdAt}}</div>
</div>
{{/each}}